# Archived planning and experiment notes

These folders are historical design/implementation notes. They are not part of
the runtime product path.

- `plans/` — older implementation plans
- `superpowers/` — design specs used during the React workbench migration
- `experiments/` — Hyperframes caption spike that later landed in
  `pixelle_video/services/hyperframes_caption_renderer.py`
